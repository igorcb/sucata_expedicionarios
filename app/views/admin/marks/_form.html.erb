<%= form_for [:admin, @mark], :html => { multipart: true, class: "form-horizontal" } do |f| %>
  <%= render 'shared/error_messages' %>
  
  <fieldset>

    <legend></legend> 

    <div class="control-group">
      <%= f.label :name, "Name", class: 'control-label'  %>
      <div class="controls">
        <%= f.text_field :name, class: 'control-label'  %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :image, "Image", class: 'control-label' %>
      <div class="controls">
        <%= f.fields_for :assets do |asset_field| %>
          <%= asset_field.file_field :asset %>
        <% end %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit 'Save', class: 'btn btn-primary' %>
      <%= link_to 'Cancel', admin_marks_path, class: 'btn' %>
    </div>

  </fieldset>

<% end %>