<!-- <h1>StaticPages#home</h1>
<p>Find me in app/views/static_pages/home.html.erb</p>
 -->
<!-- <div class="container clear-top">

</div>
 -->


<div id="gallery" data-toggle="modal-gallery" data-target="#modal-gallery">
  <% Mark.all.each do |mark| %>
    <%#= image_tag( mark.assets.first.asset.url(:thumb), :data => {:gallery => "gallery"}, :title => mark.name, class: "img-thumbnail") %>  
    <%= link_to image_tag( mark.assets.first.asset.url(:thumb), :data => {:gallery => "gallery"}, :title => mark.name, class: "img-thumbnail"), mark_path(mark) %>  
  <% end %>
</div>

<hr />

<div id="gallery" data-toggle="modal-gallery" data-target="#modal-gallery">
  <% Car.all.each do |car| %>
    <%= link_to image_tag( car.assets.first.asset.url(:thumb)), admin_car_gallery_car_path(car.id),  data: {:toggle=>"modal", :target=> "#modalProduct"} %> 
		<div id="modalProduct" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
		  <div class="modal-header">
		    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
		    <h3 id="myModalLabel"><%#= car.name.upcase %></h3>
		  </div>
		  <div class="modal-body">
	      <%#= render 'admin/cars/car_gallery', object: car, :layouts => 'false' %>	   
		  </div>
		</div>

  <% end %>
</div>

          
